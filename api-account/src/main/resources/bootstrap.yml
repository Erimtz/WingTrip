spring:
  application:
    name: api-account
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888

spring-doc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html


